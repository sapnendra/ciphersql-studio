import AuthForm from '../components/AuthForm/AuthForm';

const LoginPage = () => {
  return (
    <div className="auth-layout">
      <AuthForm mode="login" />
    </div>
  );
};

export default LoginPage;
